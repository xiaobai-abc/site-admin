// 权限列表

const AllPermissions = [0, 1];

// 普通管理员
const Ordinary = [0];

// 总管理员
const General = [1];

// 用户权限列表生成 映射
export const PermissionsMap = {
  // dashboard: AllPermissions, //首页列表
  // device: AllPermissions, //设备列表
};
